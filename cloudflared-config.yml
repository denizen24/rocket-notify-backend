# Cloudflare Tunnel конфигурация
# Используется с --token флагом, поэтому tunnel ID не нужен здесь
# Вместо этого используем переменные окружения через docker-compose

ingress:
  # Проксируем все запросы на приложение
  - service: http://app:3000
